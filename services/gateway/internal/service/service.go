// Package service is intentionally empty for the gateway service.
// The gateway delegates business logic directly to backend gRPC services
// via the proxy package; no additional service layer is needed.
package service
