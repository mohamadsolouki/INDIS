apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-govportal
  labels:
    app: govportal
spec:
  type: ClusterIP
  ports:
    - port: 8200
      targetPort: http
      name: http
    - port: 8201
      targetPort: metrics
      name: metrics
  selector:
    app: govportal
