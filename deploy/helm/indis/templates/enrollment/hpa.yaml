{{- if .Values.enrollment.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-enrollment
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-{{ .Values.global.domain | replace "." "-" }}-enrollment
  minReplicas: {{ .Values.enrollment.autoscaling.minReplicas | default 1 }}
  maxReplicas: {{ .Values.enrollment.autoscaling.maxReplicas | default 3 }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.enrollment.autoscaling.targetCPUUtilizationPercentage | default 80 }}
{{- end }}
