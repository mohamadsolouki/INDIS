{{- if .Values.biometric.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-biometric
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-{{ .Values.global.domain | replace "." "-" }}-biometric
  minReplicas: {{ .Values.biometric.autoscaling.minReplicas | default 1 }}
  maxReplicas: {{ .Values.biometric.autoscaling.maxReplicas | default 3 }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.biometric.autoscaling.targetCPUUtilizationPercentage | default 80 }}
{{- end }}
