{{- if .Values.verifier.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-verifier
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-{{ .Values.global.domain | replace "." "-" }}-verifier
  minReplicas: {{ .Values.verifier.autoscaling.minReplicas | default 1 }}
  maxReplicas: {{ .Values.verifier.autoscaling.maxReplicas | default 3 }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.verifier.autoscaling.targetCPUUtilizationPercentage | default 80 }}
{{- end }}
