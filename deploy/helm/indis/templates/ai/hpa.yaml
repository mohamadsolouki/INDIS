{{- if .Values.ai.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-ai
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-{{ .Values.global.domain | replace "." "-" }}-ai
  minReplicas: {{ .Values.ai.autoscaling.minReplicas | default 1 }}
  maxReplicas: {{ .Values.ai.autoscaling.maxReplicas | default 3 }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.ai.autoscaling.targetCPUUtilizationPercentage | default 80 }}
{{- end }}
