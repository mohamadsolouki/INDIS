{{- if .Values.ussd.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-ussd
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-{{ .Values.global.domain | replace "." "-" }}-ussd
  minReplicas: {{ .Values.ussd.autoscaling.minReplicas | default 1 }}
  maxReplicas: {{ .Values.ussd.autoscaling.maxReplicas | default 3 }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.ussd.autoscaling.targetCPUUtilizationPercentage | default 80 }}
{{- end }}
