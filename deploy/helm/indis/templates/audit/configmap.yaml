apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-audit-config
data:
  GRPC_PORT: "8080"
  METRICS_PORT: "9090"
  LOG_LEVEL: "info"
  ENVIRONMENT: "production"
  DATABASE_URL: "postgres://indis:indis@{{ .Values.global.postgres.host }}:{{ .Values.global.postgres.port }}/indis_audit?sslmode=disable"
  REDIS_URL: "redis://{{ .Values.global.redis.host }}:{{ .Values.global.redis.port }}"
  KAFKA_BROKERS: "{{ .Values.global.kafka.brokers }}"
  GRPC_TLS_MODE: "{{ if .Values.global.tls.enabled }}tls{{ else }}plaintext{{ end }}"
