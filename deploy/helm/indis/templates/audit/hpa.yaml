{{- if .Values.audit.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-audit
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-{{ .Values.global.domain | replace "." "-" }}-audit
  minReplicas: {{ .Values.audit.autoscaling.minReplicas | default 1 }}
  maxReplicas: {{ .Values.audit.autoscaling.maxReplicas | default 3 }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.audit.autoscaling.targetCPUUtilizationPercentage | default 80 }}
{{- end }}
