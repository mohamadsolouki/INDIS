apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-card
  labels:
    app: card
spec:
  type: ClusterIP
  ports:
    - port: 8400
      targetPort: http
      name: http
  selector:
    app: card
