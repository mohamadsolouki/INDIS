{{- if .Values.card.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-card
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-{{ .Values.global.domain | replace "." "-" }}-card
  minReplicas: {{ .Values.card.autoscaling.minReplicas | default 1 }}
  maxReplicas: {{ .Values.card.autoscaling.maxReplicas | default 3 }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.card.autoscaling.targetCPUUtilizationPercentage | default 80 }}
{{- end }}
