apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-card-config
data:
  HTTP_PORT: "8400"
  LOG_LEVEL: "info"
  ENVIRONMENT: "production"
  DATABASE_URL: "postgres://indis:indis@{{ .Values.global.postgres.host }}:{{ .Values.global.postgres.port }}/indis_card?sslmode=disable"
  REDIS_URL: "redis://{{ .Values.global.redis.host }}:{{ .Values.global.redis.port }}"
  KAFKA_BROKERS: "{{ .Values.global.kafka.brokers }}"
