{{- if .Values.electoral.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-electoral
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-{{ .Values.global.domain | replace "." "-" }}-electoral
  minReplicas: {{ .Values.electoral.autoscaling.minReplicas | default 1 }}
  maxReplicas: {{ .Values.electoral.autoscaling.maxReplicas | default 3 }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.electoral.autoscaling.targetCPUUtilizationPercentage | default 80 }}
{{- end }}
