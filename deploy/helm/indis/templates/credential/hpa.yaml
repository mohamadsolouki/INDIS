{{- if .Values.credential.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-credential
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-{{ .Values.global.domain | replace "." "-" }}-credential
  minReplicas: {{ .Values.credential.autoscaling.minReplicas | default 1 }}
  maxReplicas: {{ .Values.credential.autoscaling.maxReplicas | default 3 }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.credential.autoscaling.targetCPUUtilizationPercentage | default 80 }}
{{- end }}
