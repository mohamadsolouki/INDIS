{{- if .Values.justice.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-justice
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-{{ .Values.global.domain | replace "." "-" }}-justice
  minReplicas: {{ .Values.justice.autoscaling.minReplicas | default 1 }}
  maxReplicas: {{ .Values.justice.autoscaling.maxReplicas | default 3 }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.justice.autoscaling.targetCPUUtilizationPercentage | default 80 }}
{{- end }}
