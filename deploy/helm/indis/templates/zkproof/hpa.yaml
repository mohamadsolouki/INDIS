{{- if .Values.zkproof.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-zkproof
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-{{ .Values.global.domain | replace "." "-" }}-zkproof
  minReplicas: {{ .Values.zkproof.autoscaling.minReplicas | default 1 }}
  maxReplicas: {{ .Values.zkproof.autoscaling.maxReplicas | default 3 }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.zkproof.autoscaling.targetCPUUtilizationPercentage | default 80 }}
{{- end }}
